# photo-organizer

Scans fileMetadata in the specified directory, extracts images, finds duplicates, creates metainformation such as dates and tags, shows photos in sorted by date manner and filtered by tags.